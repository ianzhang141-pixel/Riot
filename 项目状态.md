# 项目状态（AI 交接文档）

> **给下一个 AI（GPT / Claude / 其他）：先读这一份，再动手。**
> 这里写的是「现在到底是什么状态」，不是计划。每次改动后必须更新这份文档和
> [CHANGELOG.md](CHANGELOG.md)。
>
> 最后更新：2026-09-06

---

## 总进度：约 18%

详见 [路线图.md](路线图.md)（含七阶段权重拆解、剩余工作的执行顺序与阻塞关系、
Riot 限流与磁盘占用的实测数字）。

**不要因为「数据链路做完了」就以为项目过半 —— 那只值 15%。**
行为抽象和因果 Q(s,a) 合计 40%，两者都还没开始。

## 一句话现状

数据链已经打通到「原始 Timeline 落档 + 分钟级 10 人 State + 肉眼核对页面」，
但 **V(s) 胜率模型尚未开始训练**，Q(s,a) 和 Regret 更没有。
当前卡点是：用户还没有用真实数据跑过一遍核对。

---

## 一、环境事实（最容易被误判的部分）

| 事实 | 说明 |
| --- | --- |
| 用户的 Mac | 所有真实数据、API Key、录像都在这里。`http://127.0.0.1:8000` 和 `:8010` 都指这台机器 |
| 云端 AI（Claude Code） | 跑在云容器里，**访问不到用户的 localhost 和硬盘**。任何本机结果只能由用户贴回来 |
| 用户本人 | **完全没有编程基础**。任何操作指导都必须精确到「按哪个键、看到什么算成功」 |

**推论：** 不要给用户 `127.0.0.1` 的链接让 AI 去看，AI 看不到。
不要假设用户知道路径、Shell、虚拟环境、端口、JSON、Git 是什么。

---

## 二、仓库里有两套代码，关系要搞清楚

### A. `lol_decision_lab_v0_1_riot_api.zip` —— GPT 写的原始项目

- 就是跑在 **8000** 端口的那个网站（FastAPI + SQLite + sklearn）
- 用户用 GitHub 网页上传的 ZIP，**没有解压进仓库**
- 数据存在 `data/lol.db`（SQLite），**不是 JSON 文件**
- 有完整的表结构、Live Client 接入、训练管线骨架

### B. `lolab/` —— Claude 写的数据工具箱

- 跑在 **8010** 端口，只用 Python 标准库，不需要 pip 装任何东西
- **只读取**项目的 `data/` 目录，不修改 GPT 那套代码
- 两者可以同时开着，互不干扰

**为什么要有 B：** GPT 那套把原始 Timeline 用完就丢，且 State 只有 1 名玩家。
B 负责把原始数据落档、抽出 10 人完整 State、并提供肉眼核对页面。

---

## 三、GPT 项目的已知问题（诊断自 ZIP 源码，尚未修复）

按严重程度排列。**这四条是接下来最该处理的技术债。**

### ① 原始 Timeline JSON 从不落盘 —— 最严重

`app/sync.py` 抓了 Timeline（`client.timeline(mid, ...)`），
但 `app/timeline.py::parse_and_store()` 当场解析后只把每帧的 `state_json`
写进 `timeline_states` 表，**原始 JSON 和每帧的 `events` 数组全部丢弃**。

后果：每改一次 State 定义，就要把所有比赛重新从 Riot 下载。
在 Development Key 24 小时过期 + 限流的前提下，几万场规模走不通。

### ② State 只有 1 名玩家，不是 10 名

`app/timeline.py` 里 `pf = pframes.get(str(pid))` —— 只取查询账号自己的
participantFrame，其余 9 人压缩成一个标量 `team_gold_diff`。

后果：**「入侵上野区值不值」这类判断做不了**，因为不知道敌方打野在哪。
与用户的目标设计（10 名玩家 × 位置/等级/金币/经验/CS/野怪）差距很大。

### ③ 明确的 bug：先锋和虚空幼虫被计成大龙

```python
elif mt in {"BARON_NASHOR","RIFTHERALD","HORDE"}: cumulative[kt]["barons"]+=1
```

`RIFTHERALD`（先锋）和 `HORDE`（虚空幼虫）混进了 `barons`。
开局吃 6 个幼虫会显示「大龙 6 条」。用这份数据训出来的 V(s) 是坏的。

### ④ Q(s,a) 是观测性拟合，不是反事实

`app/train.py` 把 `action` 当普通特征拟合 `P(赢 | state, action)`。
这会学到「回城和赢相关」而不是「回城导致赢」—— 优势方才有余裕回城。
**这正是用户明确警告过要避免的混淆陷阱**，只是形式更隐蔽。

次要：`inference.py` 里 `regret = max(0.0, best_q - actual_q)` 把 Regret
截断成非负，与用户设计稿里的 `Regret = -5.7pp` 符号约定不一致。

### 值得肯定的地方（不要推翻重做）

- `train.py` 在数据不足（<500 行 / <20 场）时**直接拒绝训练并报错**
- `inference.py` 没有模型文件时返回 0，不编造数字
- 前端显示「尚无模型曲线」是诚实的
- 数据库表结构（`analyses` 表已预留 v_win / q_json / regret）设计合理

---

## 四、`lolab/` 能做什么（已实现且验证过）

| 模块 | 作用 |
| --- | --- |
| `paths.py` | 定位项目的 `data/` 目录 |
| `net.py` | HTTPS 证书处理（解决 Mac 的 CERTIFICATE_VERIFY_FAILED） |
| `riot.diagnose()` | 自检：分清「Key 无效」与「请求被接入层拦截」两种 403 |
| `store.py` | 扫描 / 保存 Match 与 Timeline；快速盘点；自动查找 data 目录 |
| `check.py` | Timeline 完整性检查，输出中文报告 |
| `riot.py` | Riot API 客户端（Account / Match / Timeline / Key 保存与验证） |
| `ddragon.py` | 装备价格表，用于计算装备价值 |
| `state.py` | **Timeline → 分钟级 10 人 State**，严格无未来信息泄漏 |
| `server.py` + `web/console.html` | 网页控制台：数据目录 / API Key / 同步 / 补下载 |
| `web/debug.html` | 逐分钟 State 调试页（含小地图、事件中文解释） |
| `huya.py` | 虎牙录像 URL → m3u8 → 最高画质 → FFmpeg → MP4（批量 / 并发） |

### 无未来信息泄漏的保证

Timeline 里第 i 帧的 `events` 是第 i-1 帧到第 i 帧之间发生的事。
`state.build()` 严格按「先累加本帧事件，再拍下本帧快照」处理，
任何一帧的 State 只含该时刻及之前可观测的信息。

唯一例外是 `meta.blueWin` —— 训练标签，**不属于 State 特征，构造特征时必须排除**。

---

## 五、验证到了什么程度（不要夸大）

| 项目 | 状态 |
| --- | --- |
| `lolab` 全部命令与 8 个 HTTP 接口 | ✅ 用**合成数据**跑通 |
| Key 保存 / 遮蔽 / 不泄漏 / 文件权限 600 | ✅ 已验证 |
| 后台任务的成功 / 失败 / 并发保护 | ✅ 已验证 |
| **用真实 Riot 数据跑通** | ❌ **从未验证** —— 云端没有 Key，也访问不到用户硬盘 |
| 真实 Timeline 的字段完整性 | ❌ 未知，等用户跑核对 |

**任何 AI 都不要声称真实数据链已验证。**

---

## 六、进度总表

| 阶段 | 状态 |
| --- | --- |
| Riot API 连接 / Account-V1 / PUUID / Summoner-V4 | ✅ 用户已跑通（GPT 项目） |
| Match-V5 比赛列表 / 单局详情 | ✅ 用户已跑通（GPT 项目） |
| Timeline 抓取 | ✅ 两套都有（GPT 存 DB，lolab 存 JSON） |
| **原始 Timeline JSON 落档** | ✅ lolab 已实现（GPT 那套没有） |
| 网页控制台（填 Key / 选目录 / 同步） | ✅ lolab |
| Timeline → 分钟级 10 人 State | ✅ lolab |
| State 肉眼核对页面 | ✅ lolab |
| 用户在 Mac 上跑通控制台 | ✅ 已启动 |
| Riot API 认证链路（证书 / 请求头 / Key） | ✅ 全部打通，已能通过认证 |
| **用真实数据核对一遍** | ⬜ **← 当前卡在这里，等用户操作** |
| V(s) 胜率模型 | ❌ 未开始 |
| 胜率曲线 | ❌ 未开始 |
| 行为抽象 | ❌ 未开始（GPT 有粗糙版，见问题④） |
| Q(s,a) | ❌ 未开始（GPT 有观测性版本，不可用作因果推断） |
| Regret / 哪里做错 / 怎么做对 | ❌ 未开始 |
| 虎牙录像自动下载 | ✅ lolab 终端版（网页版未做） |
| 职业选手数据库 / Challenger 批量同步 | ❌ 未开始 |
| 王者荣耀 CV 线 | ❌ 未开始 |

---

## 七、下一步优先级（不要跳步）

1. **用户用真实数据跑一遍核对** ← 现在就卡在这
   小地图位置是否合理、金币等级是否单调上涨、打野的野怪列是否有数、
   击杀事件与比分是否对得上。四条都合理才算过关。
2. **修 GPT 项目的问题 ③**（先锋/幼虫算成大龙）+ **把 Timeline 改成 gzip 存储**
   （未压缩到 5 万场就是 98GB，MacBook Air 装不下 —— 见 路线图.md 第四节）
3. **把原始 Match + Timeline JSON 补档**（含 `lol.db` 里已同步过的那些）
4. **职业选手数据库 + KR Challenger 批量同步**，把样本堆到几万场
5. **只有数据量到位后，才训练第一版 V(s)**
6. V(s) 稳定后，再做 Action 抽象 → Q(s,a) → Regret

**Canyon 的 20 场只够验证链路，训不出可信模型。**
不要因为「能跑通」就急着训练，会得到一个看起来有曲线、实际没意义的模型。

---

## 八、必须避免的数据科学错误（用户的原始要求）

1. 不能因为职业选手做了某个动作就认为它是正确答案 —— 目标是比较不同 Action
   的长期价值，不是模仿 Canyon
2. 不能因为行为后 30 秒胜率下降就判定行为错误 —— 队友送头、对手神操作、
   其他线路事件都会污染因果。`ΔV` 最多只能当第一版启发式指标
3. 不能只训练赢的比赛 —— 必须包含赢/输/优势被翻/劣势翻盘，否则严重选择偏差
4. 不能泄漏未来信息 —— 10:00 的模型绝不能知道 10:30 的击杀、最终装备、最终经济
5. 职业正式比赛 ≠ 职业选手 SoloQ，必须打不同标签分开处理

---

## 九、文档索引

| 文档 | 给谁看 |
| --- | --- |
| [README.md](README.md) | 快速了解仓库 |
| [项目状态.md](项目状态.md) | **AI 交接，先读这个** |
| [路线图.md](路线图.md) | **进度百分比、剩余工作、依赖顺序、时间估计** |
| [CHANGELOG.md](CHANGELOG.md) | 每次改了什么 |
| [docs/终端怎么用.md](docs/终端怎么用.md) | 用户：终端零基础入门 |
| [docs/使用说明.md](docs/使用说明.md) | 用户：分步操作指南 |
