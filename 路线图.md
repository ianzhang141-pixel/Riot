# 路线图：整体进度与剩余工作

> 可视化版本：https://claude.ai/code/artifact/7f92f114-b365-44c7-92fd-0ba8e69e0ab7
> 最后更新：2026-09-06

---

## 一、总进度：约 18%

距离「怎么做对」可用。数据链路这一段基本打通，但它只占整个产品的一小块。
真正决定成败的「行为抽象」和「因果 Q(s,a)」还没开始 —— 这两块合起来占 40%。

权重按**实际工作量**估计，不是按条目数。

| 阶段 | 权重 | 完成度 | 贡献 |
| --- | ---: | ---: | ---: |
| 数据管道（API → Match+Timeline → 10 人 State → 核对工具） | 15% | 80% | 12.0 |
| 数据规模化（选手库、Challenger 榜、几万场同步去重） | 15% | 10% | 1.5 |
| V(s) 胜率模型（特征、训练、校准、防泄漏） | 15% | 0% | 0 |
| 胜率曲线与展示 | 5% | 0% | 0 |
| **行为抽象**（把连续操作切成可比较的离散动作） | 20% | 10% | 2.0 |
| **Q(s,a) 因果估计**（不是普通监督拟合） | 20% | 5% | 1.0 |
| Regret 与归因（最终呈现） | 10% | 10% | 1.0 |
| **合计** | **100%** | | **17.5** |

---

## 二、借一个现成胜率模型能省多少

**能省约 12 个百分点**（18% → 约 30%）：训练 V(s) 本身、生成曲线、
定位「哪里出事了」、以及为训 V(s) 而做的采集压力。

**省不掉约 50 个百分点**：行为抽象、Q(s,a)、Regret 归因，
以及细粒度 State 采集 —— 外部模型只吃粗特征（时间、经济差、人头、塔、龙），
**不知道敌方打野在哪**，而这正是「入侵值不值」所必需的。

### 一个必须纠正的概念

**V(s) 告诉你「哪里出事了」，不是「谁做错了」。**

14:32 胜率掉 6 个点是事实，但原因可能是你入侵被抓、也可能是下路同时送了两个头、
还可能是对面中单一个越塔操作。曲线本身分不清。

从「哪里出事」到「哪里做错」，中间隔的正是 Q(s,a) 和归因。

---

## 三、剩余工作与阻塞关系

标注说明：**【你】**= 只有用户能做（涉及判断、账号、或懂游戏才能定的事）；
**【AI】**= 代码工作。「需先完成 X」= 硬依赖，前置没做完这一步动不了。

### 你0 · 核对 20 场真实数据 【你】· 约 30 分钟
小地图位置是否合理、金币等级是否单调上涨、打野的野怪列有没有数、
击杀事件与比分对不对得上。**卡住全部后续工作。**

### AI1 · 修 bug + 改成压缩存储 【AI】· 约半天
先锋（RIFTHERALD）和虚空幼虫（HORDE）被算成大龙 —— GPT 版本的明确 bug。
以及把 Timeline 落档改成 gzip，见第四节存储表，这是必做项不是优化项。
> 需先完成：你0

### AI2 · 先做一版「丑但是真」的 V(s) 【AI】· 1–2 天
就用现有 20 场 + 最简单的模型，**明知道它不准**。
目的不是要准，是把「State → 胜率 → 曲线 → 前端」整条链路打通，
在花几个月采数据之前就看到产品长什么样。**这是最降风险的一步。**

### 你1 · 申请更高配额的 API Key 【你】· 申请 1 小时，审核数天到数周
Development Key 每 24 小时过期，配额只够 1500 场/小时。
要采几万场需要 Personal 或 Production Key。**越早提交越好**，别等到要用时才开始等审核。

### AI3 · 职业选手库 + Challenger 批量采集 【AI】· 写代码 1–2 天，采集 7 小时/万场
以 PUUID 为主键的选手库（改名也不丢身份）、接 Challenger/Grandmaster 榜单、
Match ID 去重、断点续传。代码不难，难在跑得久。
> 需先完成：AI1（否则采下来的数据是坏的，白跑）

### 你2 · 定义「行为」到底是什么 【你定义 + AI 实现】· 反复迭代 1–2 周
「入侵上野区」怎么从坐标和事件里定义？多长时间算一次入侵？
失败的入侵和成功的入侵算同一个动作吗？回城和「回家补装备」是一回事吗？
**这些没有标准答案，必须由懂游戏的人来定。**
> 需先完成：AI2（看过真实曲线之后，才知道该定义什么粒度）

### AI4 · Q(s,a)：从相关性走到因果 【AI】· 2–4 周
不能简单拟合「做了 A 的人赢了多少」—— 那学到的是「优势方才有余裕回城」。
需要倾向性加权 / 双重稳健 / 离线策略评估这类方法把选择偏差扣掉。
**数据量解决方差，不解决偏差**，这一步再多数据也绕不过去。
> 需先完成：你2 + AI3

### AI5 · Regret、归因与最终界面 【AI】· 3–5 天
就是设计稿里那张图：实际行为 52.1%、更优行为列表、Regret −5.7pp。
到这一步代码反而最简单，因为前面都铺好了。

---

## 四、硬约束：采集时间与磁盘占用

Development Key 限流为每 2 分钟 100 次请求；每场比赛需 2 次请求
（Match 一次、Timeline 一次），所以上限是 **25 场/分钟 = 1500 场/小时**。

| 目标场数 | 连续采集耗时 | 需刷新 Key | 未压缩 | gzip 后 |
| ---: | ---: | ---: | ---: | ---: |
| 1,000 | 0.7 小时 | 0 次 | 2 GB | 0.4 GB |
| 10,000 | 6.7 小时 | 0 次 | 20 GB | 3.5 GB |
| 50,000 | 33 小时 | 1 次 | **98 GB** | 18 GB |
| 100,000 | 67 小时 | 2 次 | **195 GB** | 35 GB |

**这张表暴露了 lolab 自己的一个问题：** 现在 `store.save()` 写的是未压缩 JSON。
采到 5 万场就是 98GB，MacBook Air 装不下。所以「改成 gzip」排进 AI1 的必做项。

**调度上的好消息：** 采集是「挂机时间」不是「工作量」。代码写完后它自己在后台跑，
可以和其他工作并行 —— 这也是为什么「你1 申请 Key」应该尽早提交。

---

## 五、总时间：约 6–10 周

前提是不发生大的推倒重来。用户本人的投入集中在两处：
**核对数据（半小时）** 和 **定义行为（1–2 周的反复讨论）**，
其余是代码时间和采集的挂机时间。

**最大的不确定性在 AI4（因果 Q）。** 有可能做出来发现估计的 Q 值噪音太大、
置信区间宽到没法用，那就要回头改行为定义或加数据。
这是研究性质的工作而非工程性质的，时间估计的可靠性明显低于其他步骤。

---

## 六、给 AI 的提醒

不要因为「数据链路做完了」就以为项目过半 —— 那只值 15%。
不要用「拟合 P(赢 | state, action)」来交付 Q(s,a)，那是相关性不是因果。
不要在用户核对真实数据之前开始训练任何模型。
