# 变更日志

> **给 AI：每次改动都必须在最上面加一条，并同步更新 [项目状态.md](项目状态.md)。**
> 格式：日期 + 做了什么 + 验证到什么程度 + 刻意没做什么。
> 最新的在最上面。

---

## 2026-09-07 · 启动时显示版本与证书状态

**为什么**

用户拉取证书修复后仍看到同样的报错。排查发现：旧的服务器进程还占着终端，
`git pull` 打在被占用的窗口里从未执行，页面上是同一条旧日志（时间戳完全相同）。
问题不在代码，在于**无法一眼判断跑的是哪个版本**。

**新增**

- `net.describe()` —— 一句话说明证书来源与 CA 数量
- 服务器启动横幅增加一行：`版本：lolab 0.1.0    ✅ 证书正常（certifi，137 个 CA）`
- `/api/status` 增加 `version` 与 `certStatus` 字段，控制台页脚同步显示

这样用户截图任何一处，都能立刻确认代码版本和证书环境，不必再靠猜。

**验证**

启动横幅、接口字段、页面页脚三处均正确显示；控制台其余功能未受影响。

---

## 2026-09-06 · 新增路线图：进度百分比、剩余工作与依赖顺序

用户问「现在做到哪了、百分之多少、还剩什么、谁做什么、要多久」。

新增 `路线图.md`（可视化版本发布为 Artifact）：

- **七阶段按工作量加权的进度拆解**，总计约 18%。数据管道虽然完成 80%，
  但它本身只占 15% 权重；行为抽象（20%）和因果 Q(s,a)（20%）几乎未动
- **借用外部胜率模型的收益评估**：约省 12pp，但省下的全是相对容易的部分
- **剩余工作按依赖排序**，明确区分「只有用户能做」与「AI 能做」，标出硬阻塞
- **实测的 Riot 限流与磁盘占用**：Dev Key 上限 25 场/分钟；5 万场未压缩 98GB

**由此发现的自身问题**

`store.save()` 目前写未压缩 JSON，采到 5 万场就是 98GB。已排进下一步必做项
（不是优化项）。

**同时纠正的一个概念**

V(s) 给出的是「哪里出事了」，不是「谁做错了」—— 胜率下跌可能来自队友。
从前者到后者中间隔的正是 Q(s,a) 和归因，这一点之前表述得不够清楚。

---

## 2026-09-06 · 修复 Mac 上的 CERTIFICATE_VERIFY_FAILED

**现象**

用户在 Mac 上点「开始同步」，日志显示：
`RiotError: 请求失败（已重试 2 次）：<urlopen error [SSL: CERTIFICATE_VERIFY_FAILED]
certificate verify failed: unable to get local issuer certificate>`

**原因**

Mac 上的 Python 默认不读系统钥匙串里的根证书，标准库 `urllib` 因此拒绝所有
HTTPS 连接。GPT 那套用 `httpx`（自带 certifi）所以不受影响，`lolab` 用标准库就中招。

**修复**

新增 `lolab/net.py`：按 certifi → 系统默认 → macOS 常见证书路径的顺序构造
SSLContext，并缓存结果。`riot.py` / `ddragon.py` / `huya.py` 的所有联网调用改走它。

`riot.get()` 另外识别证书类错误后**不再无谓重试 4 次**，直接抛出中文解决指引
（装 certifi 或运行 Install Certificates.command）。

**没有做的事**

没有关闭证书校验。`ssl_context()` 一个 CA 都找不到时会返回正常校验的 context
让它照常报错，而不是静默降级成不校验 —— 那等于把 HTTPS 的安全性关掉。
已验证返回的 context 是 CERT_REQUIRED、137 个 CA。

**验证**

全部模块编译通过；证书错误识别正确；控制台 4 个接口仍返回 200；
非证书类的网络错误仍走原来的重试路径。

---

## 2026-09-06 · 交接文档与终端入门指南

**新增**

- `项目状态.md` —— AI 交接文档，单一事实来源。包含环境事实、两套代码的关系、
  GPT 项目的四个已知问题、验证程度、进度总表、下一步优先级
- `CHANGELOG.md` —— 本文件
- `docs/终端怎么用.md` —— 写给完全没用过终端的人：打开终端、什么是提示符、
  怎么复制粘贴执行、五种常见情况怎么分辨、必须记住的三个快捷键

**为什么**

用户完全没有编程基础，看不懂终端操作；同时需要一个地方让 GPT 和 Claude
都能最快看懂当前项目进度，避免每轮重新解释。

**刻意没做**

V(s)、胜率曲线、Q(s,a)、Regret 仍未实现。等真实数据核对通过再开始。

---

## 2026-09-06 · 网页控制台：填 API Key、选目录、同步 Timeline 全部可视化

**背景**

用户完全没有编程基础，原流程要求在终端敲带路径参数的命令，门槛太高。
本轮把整条数据链搬进浏览器，终端只剩一条启动命令。

**新增**

- `web/console.html` —— 三步式控制台：1 数据目录（支持自动查找）→
  2 粘贴并验证 API Key → 3 按 Riot ID 同步最近 N 场。
  底部比赛库逐场标注 Timeline 齐不齐，缺的可单场补下载
- `riot.py` —— `verify_key`（轻量接口验证 Key）、`fetch_account`（Riot ID → PUUID）、
  `fetch_match_ids`、`save_api_key`（权限 600）、`mask`（遮蔽显示）
- `store.py` —— `inventory()` 快速盘点每场 Match / Timeline 齐不齐
  （优先用文件名 + 开头 16KB 判断类型，避免为列清单解析几百 MB）；
  `find_data_dir_candidates()` 扫描个人文件夹找 data 目录
- `server.py` —— 重写。POST 路由、可切换数据目录、带进度和日志的后台任务
  （同时只允许一个，异常会被捕获显示）。共 9 个接口

**删除**

- `web/index.html` —— 被 console.html 取代

**验证**

合成数据跑通全部 8 个 HTTP 接口（均 200）、Key 保存与遮蔽往返、
确认完整 Key 不出现在任何接口返回里、文件权限 600、
后台任务的成功/失败/并发保护三条路径、原有命令行子命令未被破坏。

**仍未用真实 Riot 数据验证** —— 云端没有 Key，也访问不到用户硬盘。

---

## 2026-09-06 · 合并用户上传的 GPT 初版 ZIP

用户通过 GitHub 网页上传了 `lol_decision_lab_v0_1_riot_api.zip`
（就是跑在 8000 端口的那个 FastAPI 项目）。与本地分支合并，保留双方内容。

**由此得到的诊断**（详见 [项目状态.md](项目状态.md) 第三节）：

1. 原始 Timeline JSON 从不落盘，只存派生的 `state_json` 到 SQLite —— 最严重
2. State 只有 1 名玩家，不是 10 名 —— 支撑不了「入侵上野区」这类判断
3. 明确 bug：先锋（RIFTHERALD）和虚空幼虫（HORDE）被计成大龙
4. Q(s,a) 是观测性拟合而非反事实，正是用户警告过的混淆陷阱

**同时确认 GPT 做对的地方**：数据不足时拒绝训练、没有模型时不编造数字、
表结构设计合理。不要推翻重做。

---

## 2026-09-06 · 补齐 lolab 的命令行入口与 State 调试页面

上一轮已写好数据扫描、Timeline 检查、State 抽取、虎牙下载和调试服务器，
但缺两块拼图导致整个包跑不起来：

- 缺 `__main__.py` —— 所有文档里写的 `python3 -m lolab ...` 命令都不存在
- 缺 `web/debug.html` —— server.py 的 `/match/<id>` 路由要用

**新增**

- `__main__.py` —— where / check / fetch / state / serve / items / huya 七个子命令
- `web/debug.html` —— 分钟级 State 调试页：时间滑块、10 名玩家逐帧快照、
  双方塔龙先锋大龙与经济差、每条事件的中文解释、把 Timeline x/y 画出来的小地图
- `README.md`、`docs/使用说明.md`

**验证**

合成的 Match + Timeline 跑通 where / check / state --minute / serve 全部路径，
调试服务器 4 个接口均返回 200。
