"""LOL PC 决策实验室 —— 数据验证与状态抽取工具箱。

设计原则：
1. 只用 Python 标准库，Mac 自带 python3 即可运行，不需要 pip 安装任何东西。
2. 不修改、不依赖现有项目结构，只读取 data/ 目录里的 JSON / SQLite。
3. 先验证真实数据，再谈模型。本包内不存在任何“假装已经训练好”的胜率模型。
"""

__version__ = "0.1.0"
